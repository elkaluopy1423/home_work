/*There will be an error because Dart is a statically typed language  
so the compiler needs to know the variable’s type before using it.*/

void main() {
  print(age); //  Error because The variable age is used before being declared
  int age = 25;
}
